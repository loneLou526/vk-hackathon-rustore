# 🏔️ RuStore Expedition — Геймифицированная витрина приложений

RuStore Expedition — это интерактивная веб-платформа с геймификацией, системой уровней, XP, достижений, внутренней валютой и магазином наград.  
Проект создан для увеличения вовлечённости пользователей каталога приложений за счёт игровых механик, сценариев прогрессии и интерактивной обратной связи.
Ссылка на рабочий сайт: http://185.119.59.9/
---

## ✨ Возможности

| Категория | Функционал |
|----------|-------------|
| 🧩 Геймификация | уровни, XP, внутренняя валюта (пиксели), достижения, задания |
| 🎡 Мотивационные механики | колесо удачи, магазин наград |
| 🛍️ Витрина приложений | каталог, карточки приложений, отзывы |
| 👤 Авторизация | регистрация, вход, JWT-сессии, профиль |
| ⚙️ Backend-логика | FastAPI, Alembic, PostgreSQL, JWT |
| 🌐 Frontend | React + Vite + Zustand + Axios |

---

## 🏗️ Стек технологий

### Frontend
- React 18 + TypeScript
- Vite
- Zustand
- React Router
- Axios
- Nginx (prod runtime)

### Backend
- Python 3.11
- FastAPI
- SQLAlchemy ORM
- Alembic
- PostgreSQL
- python-jose (JWT)
- passlib (bcrypt)

### Infrastructure
- Docker + Docker Compose
- Nginx reverse-proxy
- Production multi-stage build

---


---

## 🚀 Установка и запуск (локально, без Docker)

### 1️⃣ Backend

```bash

cd server
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload
```
Backend: http://localhost:8000

Swagger Docs: http://localhost:8000/docs
### 2️⃣ Frontend
```bash

cd client
npm install
npm run dev
```
Frontend: http://localhost:5173

### 🐳 Production запуск (Docker)

Перед запуском убедитесь, что установлены:
```
Docker >= 20
Docker Compose >= 2
```

Основной сценарий
```
docker compose build
docker compose up -d
```

Проверка:
```
docker compose ps
```

Сайт будет доступен по адресу (внешний IP сервера):

`http://<SERVER_IP>/`

### 🔐 Авторизация и токены

* JWT хранится в localStorage

* Авторизационный заголовок:

`Authorization: Bearer <token>`

### 🗄️ Миграции базы данных

Создать новую миграцию:

`alembic revision --autogenerate -m "migration message"
`

Применить миграции:

`alembic upgrade head`

### 📦 Резервное копирование и восстановление БД
Экспорт в .dump:
`pg_dump -U USER -d DBNAME -Fc -f backup.dump
`
Восстановление:
`pg_restore -U USER -d DBNAME backup.dump`

### 🧭 Roadmap

*  Анимации и микровзаимодействия
*  Лента игровых событий и наград
*  Сезонные квест-пасы

*  PvP-соревнования и таблица лидеров

*  Push-уведомления и триггеры вовлечения

*  Аналитика ClickHouse + Events Dashboard

### 📬 Контакты и сотрудничество

Мы открыты к партнёрствам и развитию!
Если хотите обсудить интеграции, идеи или инвестирование — пишите:

Telegram: `@scr1ptSorc404`

Email: `ibragimmuradov03@gmail.com`
